{
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    // Add any additional compiler options here
  },
  "include": [
    "src/**/*.ts",
    "test/**/*.ts",
    // Add other paths based on your project structure
  ],
  "exclude": [
    "node_modules"
  ]
}
